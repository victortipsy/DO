export enum HTTPCODE {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  CONFLICT = 409,
  UNPROCESSIBLE_IDENTITY = 422,
  INTERNAL_SERVER_ERROR = 500,
}
